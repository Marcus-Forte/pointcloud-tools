name: Duna Pointcloud Tools

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build-and-test:
    runs-on:
      group: duna-pool

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      - name: Build
        run: ./docker_build.sh

      - name: Test
        run: docker run duna-pointcloud-tools /app/build/scan_matching/tst_scan_matching